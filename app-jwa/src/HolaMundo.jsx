import React from "react";

// principal de nuestro objeto o componente
function HolaMundo(params) {
    return(
        <div>
           Hola Mundo desde un archivo JSX 
        </div>
    );
}

export default HolaMundo;